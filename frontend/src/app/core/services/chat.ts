import{inject,Injectable}from'@angular/core';import{HttpClient,HttpParams}from'@angular/common/http';import{Observable}from'rxjs';import{environment}from'../../../environments/environment';export interface ChatUser{id:string;username:string;avatarUrl:string|null}export interface ChatMessage{id:string;content:string|null;author:ChatUser;createdAt:string;editedAt:string|null;deletedAt:string|null}export interface Conversation{id:string;type:'DIRECT'|'FACTION';title:string;avatarUrl:string|null;participants:ChatUser[];faction:{id:string;name:string}|null;lastMessage:ChatMessage|null;lastMessageAt:string|null;unreadCount:number}export interface MessagePage{items:ChatMessage[];nextCursor:string|null;limit:number}@Injectable({providedIn:'root'})export class ChatService{#http=inject(HttpClient);#url=`${environment.apiUrl}/api/conversations`;list():Observable<{items:Conversation[]}>{return this.#http.get<{items:Conversation[]}>(this.#url)}get(id:string){return this.#http.get<Conversation>(`${this.#url}/${id}`)}direct(userId:string){return this.#http.post<Conversation>(`${this.#url}/direct`,{userId})}messages(id:string,cursor?:string){let p=new HttpParams().set('limit',30);if(cursor)p=p.set('cursor',cursor);return this.#http.get<MessagePage>(`${this.#url}/${id}/messages`,{params:p})}send(id:string,content:string){return this.#http.post<ChatMessage>(`${this.#url}/${id}/messages`,{content})}edit(id:string,m:string,content:string){return this.#http.patch<ChatMessage>(`${this.#url}/${id}/messages/${m}`,{content})}delete(id:string,m:string){return this.#http.delete<void>(`${this.#url}/${id}/messages/${m}`)}read(id:string){return this.#http.post<void>(`${this.#url}/${id}/read`,{})}unreadCount(){return this.#http.get<{unreadCount:number}>(`${this.#url}/unread-count`)}faction(id:string){return this.#http.get<Conversation>(`${environment.apiUrl}/api/factions/${id}/conversation`)}}
