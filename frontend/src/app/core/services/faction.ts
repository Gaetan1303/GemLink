import{inject,Injectable}from'@angular/core';import{HttpClient,HttpParams}from'@angular/common/http';import{Observable}from'rxjs';import{environment}from'../../../environments/environment';export type FactionVisibility='PUBLIC'|'PRIVATE';export type FactionRole='OWNER'|'OFFICER'|'MEMBER';export interface FactionUser{id:string;username:string;avatarUrl?:string|null}export interface FactionMember{user:FactionUser;role:FactionRole;joinedAt:string}export interface Faction{id:string;name:string;slug:string;description:string|null;visibility:FactionVisibility;status:'ACTIVE'|'ARCHIVED';avatarUrl?:string|null;bannerUrl?:string|null;memberCount?:number;owner:FactionUser|null;membership?:{role:FactionRole}|null;permissions?:string[]}export interface FactionPage{items:Faction[];nextCursor:string|null;limit:number}export interface JoinRequest{id:string;requester:FactionUser;message:string|null;createdAt:string}@Injectable({providedIn:'root'})export class FactionService{#http=inject(HttpClient);#url=`${environment.apiUrl}/api/factions`;list(o:{cursor?:string;search?:string;visibility?:FactionVisibility;mine?:boolean}={}):Observable<FactionPage>{let p=new HttpParams().set('limit',20);if(o.cursor)p=p.set('cursor',o.cursor);if(o.search)p=p.set('search',o.search);if(o.visibility)p=p.set('visibility',o.visibility);if(o.mine)p=p.set('membership','mine');return this.#http.get<FactionPage>(this.#url,{params:p})}get(id:string){return this.#http.get<Faction>(`${this.#url}/${id}`)}create(d:{name:string;description:string;visibility:FactionVisibility}){return this.#http.post<Faction>(this.#url,d)}members(id:string){return this.#http.get<{items:FactionMember[]}>(`${this.#url}/${id}/members`)}join(id:string){return this.#http.post<{status:string}>(`${this.#url}/${id}/join`,{})}leave(id:string){return this.#http.post(`${this.#url}/${id}/leave`,{})}requests(id:string){return this.#http.get<{items:JoinRequest[]}>(`${this.#url}/${id}/join-requests`)}review(id:string,r:string,d:'accept'|'reject'){return this.#http.post(`${this.#url}/${id}/join-requests/${r}/${d}`,{})}archive(id:string){return this.#http.delete(`${this.#url}/${id}`)}}
