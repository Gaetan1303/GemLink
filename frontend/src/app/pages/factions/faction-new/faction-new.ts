import { Component, inject, signal } from '@angular/core';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';import { Router } from '@angular/router';import { FactionService, FactionVisibility } from '../../../core/services/faction';import { SharedModule } from '../../../shared/shared-module';
@Component({selector:'app-faction-new',imports:[SharedModule,ReactiveFormsModule],templateUrl:'./faction-new.html',styleUrls:['./faction-new.scss']})export class FactionNew{readonly #fb=inject(FormBuilder);readonly #router=inject(Router);readonly #factions=inject(FactionService);protected readonly submitting=signal(false);protected readonly error=signal<string|null>(null);protected readonly form=this.#fb.nonNullable.group({name:['',[Validators.required,Validators.minLength(3),Validators.maxLength(100)]],description:['',[Validators.maxLength(2000)]],visibility:['PUBLIC' as FactionVisibility,Validators.required]});protected submit():void{this.form.markAllAsTouched();if(this.form.invalid||this.submitting())return;this.submitting.set(true);this.#factions.create(this.form.getRawValue()).subscribe({next:f=>this.#router.navigate(['/factions',f.id]),error:e=>{this.error.set(e.error?.message??'Création impossible.');this.submitting.set(false);}});}}
